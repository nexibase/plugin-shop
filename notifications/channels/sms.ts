import type { NotificationPayload } from '../send'

// Stub — Task 15 implements CoolSMS integration with global + user-level opt-in.
export async function send(_payload: NotificationPayload): Promise<void> {
  // Not yet implemented.
}
